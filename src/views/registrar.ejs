<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Troca de livros</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/css">
</head>
<body class="bg-white">
    <div>
        <%- include('Headers/headerPublic.ejs') %>
    </div>
    <div class="container-fluid mb-3">
        <div class="row mt-5">
            <div class="col-12">
                <h1 class="text-center fw-bold mt-5 mb-5 text-primary">Realize seu cadastro</h1>
            </div>
            <div class="col-2"></div>
            <div class="col-8">
                <div class="border border-primary border-4 shadow shadow-lg rounded p-3 pt-2">
                    <form action="/registrar" method="post">
                        <label class="form-label mt-2 text-dark fw-bold" for="userClient">Nome</label>
                        <input type="text" class="form-control" id="userClient" name="userClient" placeholder="Digite o seu nome..." required>
                        <label class="form-label mt-2 text-dark fw-bold" for="emailClient">Email</label>
                        <input type="email" class="form-control" id="emailClient" name="emailClient" placeholder="Digite o seu melhor e-mail..." required>
                        <label class="form-label mt-2 text-dark fw-bold" for="passClient">Senha</label>
                        <input type="password" class="form-control" id="passClient" name="passClient" placeholder="Digite a sua melhor senha..." required>
                        <label class="form-label mt-2 text-dark fw-bold" for="birthClient">Data de Nascimento</label>
                        <input type="date" class="form-control" id="birthClient" name="birthClient" required>
                        <label class="form-label mt-2 text-dark fw-bold" for="phoneClient">Celular</label>
                        <input type="tel" class="form-control" id="phoneClient" name="phoneClient" pattern="[0-9]{11}" placeholder="Ex: (43) 984160945" required maxlength="11">
                        <button class="btn btn-primary mt-3 text-white fw-bold" type="submit">Registrar</button>
                    </form>
                </div>
            </div>
            <div class="col-2"></div>
        </div>
    </div>
</body>
</html>