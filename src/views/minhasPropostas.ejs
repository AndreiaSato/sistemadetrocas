<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>TrocaLivros - Minhas propostas</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/css">
</head>
<body class="bg-white">
    <div> 
        <%- include ('Headers/headerPrivate.ejs') %>
    </div>
    <div class="container-fluid mb-3">
        <div class="row mt-5">
            <div class="col-12">
                <h1 class="text-center fw-bold mt-5 mb-5 text-primary">Minhas propostas</h1>
            </div>
            <div class="col-2"></div>
            <div class="col-8">
                <div class="border border-primary border-4 shadow shadow-lg rounded p-2 m-0">
                    <table class="table table-bordered table-striped shadow shadow-lg m-0">
                        <thead class="thead-dark">
                            <tr>
                                <th class="bg-primary text-white">ID</th>
                                <th class="bg-primary text-white">Nome do livro</th>
                                <th class="bg-primary text-white">Dono do livro</th>
                                <th class="bg-primary text-white">Proposto</th>
                                <th class="bg-primary text-white">Data da Proposta</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% proposals.forEach(proposal => { %>
                                <tr>
                                    <td class="bg-light fw-semibold"><%= proposal.idProposalBook %></td>
                                    <td class="bg-light fw-semibold"><%= proposal.bookTitle %></td>
                                    <td class="bg-light fw-semibold"><%= proposal.ownerName %></td>
                                    <td class="bg-light fw-semibold"><%= proposal.proposerName %></td>
                                    <td class="bg-light fw-semibold"><%= proposal.createdProposal %></td>
                                </tr>
                            <% }); %>
                        </tbody>
                    </table>
                </div>
            </div>
            <div class="col-2"></div>
        </div>
    </div>
</body>
</html>